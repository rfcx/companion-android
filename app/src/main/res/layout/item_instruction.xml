<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/margin_padding_small">

    <View
        android:id="@+id/instructionCircle"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/circle_instruction"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/instructionNumberText"
        style="@style/AudioMothText.Body.Secondary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_primary"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/instructionCircle"
        app:layout_constraintEnd_toEndOf="@id/instructionCircle"
        app:layout_constraintStart_toStartOf="@id/instructionCircle"
        app:layout_constraintTop_toTopOf="@id/instructionCircle"
        tools:text="1" />

    <TextView
        android:id="@+id/instructionDescText"
        style="@style/AudioMothText.Body.Secondary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_padding_normal"
        android:textColor="@color/text_primary"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/instructionCircle"
        app:layout_constraintStart_toEndOf="@id/instructionCircle"
        app:layout_constraintTop_toTopOf="@id/instructionCircle"
        tools:text="Powering . . . ." />

</androidx.constraintlayout.widget.ConstraintLayout>
