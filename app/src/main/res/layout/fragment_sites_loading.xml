<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent"
    android:orientation="vertical">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/bg_instruction"
        android:padding="@dimen/margin_padding_normal">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/siteLoadingText"
            style="@style/AudioMothText.Body.Secondary"
            android:text="@string/sites_loading_dialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textAlignment="center"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/siteLoadingButton"
            style="@style/AudioMothButton.GreenButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_padding_normal"
            android:text="@string/got_it"
            app:layout_constraintEnd_toEndOf="@id/siteLoadingText"
            app:layout_constraintStart_toStartOf="@id/siteLoadingText"
            app:layout_constraintTop_toBottomOf="@id/siteLoadingText" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>
